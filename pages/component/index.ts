export interface Context {
  component: {
    context: string;
    draftHref: string;
    isDraft: boolean;
    isEditable: string;
    manageHref: string;
    publishHref: string;
    renderHref: string;
    script: string;
    template: string;
    title: string;
  };
}
